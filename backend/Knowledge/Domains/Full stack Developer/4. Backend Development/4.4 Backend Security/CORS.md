# CORS

- Browser-enforced cross-origin read policy
- Allowed origins, methods, headers và credentials
- Preflight requests
- Không dùng wildcard origin với credentials
- CORS không phải authentication hoặc server-to-server firewall
