# Utility Types

- `Partial`, `Required`, `Readonly`, `Pick`, `Omit`, `Record`
- `Exclude`, `Extract`, `NonNullable`, `ReturnType` và `Parameters` ở mức cần thiết
- Utility types tạo type mới, không transform runtime values
