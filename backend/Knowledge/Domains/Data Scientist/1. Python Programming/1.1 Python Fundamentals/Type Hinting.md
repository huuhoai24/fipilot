# Type Hinting

- Parameter và return annotations
- Built-in generics như `list[str]` và `dict[str, int]`
- `Optional`, `Union`, `Literal` và `Any` ở mức cần thiết
- Static type checking ở mức khái niệm
- Type hints không được Python runtime tự động enforce
