# Normalization

- Min-max scaling về một khoảng xác định, thường là `[0, 1]`
- Công thức dựa trên minimum và maximum của training data
- Nhạy với outliers
- Xử lý giá trị ngoài training range khi inference
- Phân biệt feature normalization với vector L1/L2 normalization
