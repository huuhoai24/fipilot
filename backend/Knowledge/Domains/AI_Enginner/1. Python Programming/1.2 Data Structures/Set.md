# Set

- Unordered collection của unique hashable elements
- Membership testing
- `add()`, `remove()`, `discard()`, `update()`
- Union, intersection, difference và symmetric difference
- `frozenset`
- Complexity của membership và update
