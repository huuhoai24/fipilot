# List

- Ordered và mutable sequence
- Indexing và negative indexing
- Slicing và slice step
- `append()`, `extend()`, `insert()`, `remove()`, `pop()`
- `sort()` so với `sorted()`
- List comprehension
- Nested lists
- Copy vs Deep Copy
- Shallow copy và deep copy
- Complexity của indexing, append, insert, delete và membership
