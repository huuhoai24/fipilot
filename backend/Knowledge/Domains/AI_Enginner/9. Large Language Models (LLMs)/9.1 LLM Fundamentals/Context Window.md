# Context Window

- Tổng context model có thể xử lý theo giới hạn cụ thể
- System, user, retrieved content và generated tokens cùng dùng budget
- Long context không bảo đảm model dùng mọi thông tin hiệu quả
- Truncation và context prioritization
