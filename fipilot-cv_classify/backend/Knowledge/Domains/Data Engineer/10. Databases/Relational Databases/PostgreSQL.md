# PostgreSQL

- Relational database with rich SQL and extensibility
- MVCC, indexing and transactions at a high level
- JSON support does not make schema design unnecessary
- Operational and analytical use depends on workload
