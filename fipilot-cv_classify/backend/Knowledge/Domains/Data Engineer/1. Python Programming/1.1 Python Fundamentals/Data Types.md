# Data Types

- Numeric types: `int`, `float`, `complex`
- Boolean, string, bytes và `None`
- Container types ở mức tổng quan
- Type inspection bằng `type()` và `isinstance()`
- Type conversion và coercion
- Floating-point precision và overflow considerations
