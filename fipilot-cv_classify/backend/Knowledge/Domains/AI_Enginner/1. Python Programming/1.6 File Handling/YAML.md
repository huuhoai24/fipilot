# YAML

- Mapping, sequence và scalar
- `yaml.safe_load()`
- `yaml.safe_dump()`
- YAML không thuộc Python Standard Library; thường dùng PyYAML
- Cẩn thận type coercion và cấu hình không hợp lệ
- Không dùng unsafe loader với dữ liệu không tin cậy
