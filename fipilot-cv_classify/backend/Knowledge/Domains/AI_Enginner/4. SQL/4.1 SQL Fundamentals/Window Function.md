# Window Function

- `OVER()`
- `PARTITION BY` và `ORDER BY`
- `ROW_NUMBER`, `RANK`, `DENSE_RANK`
- Running totals và moving windows
- `LAG` và `LEAD`
- Window functions không collapse rows như `GROUP BY`
