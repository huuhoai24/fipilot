# Secure Coding

- Treat external input as untrusted
- Least privilege
- Safe defaults và fail closed
- Avoid insecure deserialization và unsafe dynamic execution
- Keep dependencies and runtimes patched
