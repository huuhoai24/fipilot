# Dependency Injection

- Dependency được cung cấp từ bên ngoài
- Constructor injection là lựa chọn phổ biến cho required dependencies
- Lifecycle và scope của dependency
- Testability và replaceability
- Avoid service locator và hidden dependencies
