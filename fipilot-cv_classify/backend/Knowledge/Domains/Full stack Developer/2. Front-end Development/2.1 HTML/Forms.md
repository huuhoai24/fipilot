# Forms

- `form`, `label`, `input`, `textarea`, `select`, `option` và `button`
- Input types
- `name`, `value`, `action` và `method`
- Native validation với `required`, `pattern`, `min`, `max` và related attributes
- Form submission và encoding
- Label association và accessible error messages
- Client-side validation không thay thế server-side validation
