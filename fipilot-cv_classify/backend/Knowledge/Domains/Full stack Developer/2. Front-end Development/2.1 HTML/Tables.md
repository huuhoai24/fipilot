# Tables

- `table`, `caption`, `thead`, `tbody`, `tfoot`, `tr`, `th` và `td`
- Header relationships với `scope`
- Tables dùng cho tabular data, không dùng để bố trí layout
- Responsive handling cho bảng rộng
