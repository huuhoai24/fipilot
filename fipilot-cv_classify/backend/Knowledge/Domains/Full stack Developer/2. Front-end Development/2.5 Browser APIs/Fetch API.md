# Fetch API

- HTTP request và Promise-based response
- Headers, body và content types
- Fetch chỉ reject cho network-level failures; HTTP error status cần được kiểm tra
- Abort với `AbortController`
- Credentials, CORS và cache options
- Streaming ở mức khái niệm
