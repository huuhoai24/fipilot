# Cookies

- Cookie attributes: `Secure`, `HttpOnly`, `SameSite`, domain, path và lifetime
- Session cookie và persistent cookie
- CSRF considerations
- Không lưu sensitive plaintext data trong cookie
