# Dockerfile

- `FROM`, `WORKDIR`, `COPY`, `RUN`, `ENV`, `CMD`/`ENTRYPOINT`
- Layer caching
- Dependency installation trước source copy khi phù hợp
- Multi-stage builds
- `.dockerignore`
- Non-root user
