# Text File

- `open()`
- Modes: `r`, `w`, `a`, `x`, `b`, `t`, `+`
- `read()`, `readline()`, `readlines()`
- Iteration theo từng dòng
- `write()` và `writelines()`
- Encoding, thường dùng UTF-8
- `seek()` và `tell()`
- Xử lý file lớn theo streaming
