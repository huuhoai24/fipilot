# JSON

- JSON objects, arrays, strings, numbers, booleans và null
- `json.load()` và `json.dump()`
- `json.loads()` và `json.dumps()`
- Serialization và deserialization
- Encoding Unicode
- Custom serialization cho type không hỗ trợ trực tiếp
