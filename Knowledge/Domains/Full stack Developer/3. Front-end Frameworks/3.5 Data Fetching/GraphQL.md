# GraphQL

- Schema, query, mutation và variables
- Client chỉ định fields cần lấy
- Errors có thể tồn tại cùng partial data
- Caching khó hơn nếu không chuẩn hóa identities
- Query cost và authorization
