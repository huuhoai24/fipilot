# Service Discovery

- Kubernetes mechanisms for resolving Services and pod endpoints through DNS and service abstractions
- Namespaces, ClusterIP, headless Services, endpoint updates, DNS caching, and readiness
